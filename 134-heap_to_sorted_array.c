#include "binary_trees.h"

/**
 * count_nodes - Counts the number of nodes in the heap
 * @root: Pointer to the root node of the heap
 *
 * Return: Number of nodes in the heap
 */
int heap_extract(heap_t **root)
{

}
